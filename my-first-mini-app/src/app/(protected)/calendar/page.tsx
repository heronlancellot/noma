import type { Metadata } from 'next';
import { CalendarPage } from '@/features/calendar';

export const metadata: Metadata = {
  title: 'Calendar | NOMA',
  description: 'Your upcoming and past experiences on NOMA.',
};

export default CalendarPage;
