import type { Metadata } from 'next';
import { ProfilePage } from '@/features/profile';

export const metadata: Metadata = {
  title: 'Profile | NOMA',
  description: 'View your NOMA profile, stats, missions and NFT gallery.',
};

export default ProfilePage;
